{
  "name": "sks-subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy": "graph deploy --product hosted-service samkingco/sks subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "0.28.1"
  }
}
